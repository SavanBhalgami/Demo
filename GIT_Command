git init :- Initilize the project directory [git init]

git add :- Add the file into repository [git add filename.filetype]

git commit :- Any change in the file you need commit [git commit -m 

git status :- File is Staging are or not or find the status [git status]

git log :- View all commit into log with author and date [git log]

git branch :- display all brach in the current repository [git branch]

git delete branch :- delete specific branch [git branch -d branch_name]

git pull :- latest Changes in the git repository (remote repo) you can download modify into local repository [git pull]

git restore :- In local repo if file is deleted but you can restore the file from remote repo [git restore filename.filetype]
